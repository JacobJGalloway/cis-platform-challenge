services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: cis
      POSTGRES_PASSWORD: cis
      POSTGRES_DB: cis_platform
    ports:
      - "5432:5432"
    volumes:
      - cis_pg:/var/lib/postgresql/data

volumes:
  cis_pg:
